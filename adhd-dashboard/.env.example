# ============================================
# 🧠 ADHD Dashboard v2.0 - Environment Variables
# ============================================
# این فایل را کپی کنید به .env و مقادیر را پر کنید:
# cp .env.example .env
# ============================================

# --------------------------------------------
# 🔐 FLASK SETTINGS
# --------------------------------------------

# کلید امنیتی Flask برای session ها
# برای تولید: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here-generate-a-random-one

# حالت اجرا: development یا production
FLASK_ENV=development

# پورت اجرای برنامه (پیش‌فرض: 5000)
FLASK_PORT=5000

# فعال/غیرفعال کردن حالت Debug
DEBUG=True

# --------------------------------------------
# 📝 NOTION API
# --------------------------------------------

# توکن API نوشن (Internal Integration Token)
# نحوه دریافت:
# 1. برو به https://www.notion.so/my-integrations
# 2. روی "New integration" کلیک کن
# 3. نام: ADHD Dashboard
# 4. Workspace خودت رو انتخاب کن
# 5. توکن رو کپی کن (شروع میشه با secret_)
NOTION_API_KEY=secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ✨ شناسه صفحه Parent برای ساخت Database های جدید
# نحوه دریافت:
# 1. یک صفحه خالی در Notion بساز (مثلا "🧠 ADHD System")
# 2. صفحه رو Share کن با Integration (... → Connections → ADHD Dashboard)
# 3. لینک صفحه رو کپی کن: https://www.notion.so/PAGE_NAME-PAGE_ID
# 4. PAGE_ID رو بذار (32 کاراکتر آخر بدون خط تیره)
# مثال: اگه لینک این باشه: https://www.notion.so/My-Page-abc123def456...
# فقط abc123def456... رو بذار
NOTION_PARENT_PAGE_ID=

# شناسه Database ها (بعد از Sync خودکار پر میشن)
# اگه از قبل داری، اینجا بذار. اگه نه، خالی بذار تا Sync بسازه
NOTION_TASKS_DB_ID=
NOTION_PROJECTS_DB_ID=
NOTION_RESOURCES_DB_ID=
NOTION_DAILY_LOGS_DB_ID=
NOTION_HABITS_DB_ID=

# --------------------------------------------
# 📊 GOOGLE SHEETS API
# --------------------------------------------

# مسیر فایل credentials.json
# نحوه دریافت:
# 1. برو به https://console.cloud.google.com
# 2. یک پروژه جدید بساز یا انتخاب کن
# 3. APIs & Services → Enable APIs → Google Sheets API فعال کن
# 4. APIs & Services → Credentials → Create Credentials → Service Account
# 5. نام بده و بساز
# 6. روی Service Account کلیک کن → Keys → Add Key → Create new key → JSON
# 7. فایل دانلود شده رو بذار کنار پروژه و مسیرش رو بنویس
GOOGLE_SHEETS_CREDENTIALS=./credentials.json

# شناسه Google Sheet برای Daily Log
# نحوه دریافت:
# 1. Sheet رو باز کن
# 2. از URL کپی کن: docs.google.com/spreadsheets/d/SHEET_ID/edit
# 3. فقط SHEET_ID رو بذار
# مهم: Sheet رو با email سرویس اکانت Share کن (از credentials.json → client_email)
DAILY_LOG_SHEET_ID=

# نام شیت (Tab) داخل فایل (پیش‌فرض: Sheet1)
DAILY_LOG_SHEET_NAME=Sheet1

# --------------------------------------------
# 🤖 TELEGRAM BOT (اختیاری - فاز بعد)
# --------------------------------------------

# توکن بات تلگرام برای یادآوری
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --------------------------------------------
# ⚙️ APP SETTINGS
# --------------------------------------------

# نام کاربر برای نمایش در Dashboard
USER_NAME=کاربر

# تعداد آیتم در هر صفحه (Pagination)
ITEMS_PER_PAGE=10

# مسیر دیتابیس SQLite برای cache
DATABASE_PATH=./database/local.db
